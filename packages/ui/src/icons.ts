/**
 * Optional tree-shakeable OntologyX icon pack.
 *
 * Keep this entry separate from the canonical `@ontologyx/ui` entry so
 * applications only pay for the glyphs they import.
 */
export * from './icons/index';
